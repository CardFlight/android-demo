<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardflight">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" /> <!-- automatic -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- automatic -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" /> <!-- automatic -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" /> <!-- automatic -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- automatic -->

    <supports-screens android:largeScreens="true" />


    <application
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>